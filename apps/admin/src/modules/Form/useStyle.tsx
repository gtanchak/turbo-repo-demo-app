import { css } from '@emotion/css';

const FormListWrapper = css`
  .header {
    button {
      height: unset;
      line-height: 20px;
      padding: 8px 16px;
      letter-spacing: unset;
      font-size: 0.875em;
      font-weight: 600;
    }
    padding-top: 10px;
    flex-direction: row;
    padding: 24px 24px 0px 24px;
    align-items: center;
    justify-content: space-between;
  }
`;
export default FormListWrapper;
